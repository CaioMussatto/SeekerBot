services:
  jobseeker:
    build: .
    ports:
      - "8080:8080"
    environment:
      - USE_DB=False  
      - PORT=8080
    env_file:
      - .env
    labels:
      - "org.opencontainers.image.title=SeekerBot"
      - "org.opencontainers.image.authors=Caio Mussatto <caio.mussatto@gmail.com>"